[build]
  publish = "."
  functions = "netlify/functions"

[functions]
  node_bundler = "esbuild"

[functions."claude"]
  timeout = 26

[functions."claude-background"]
  timeout = 900

[[headers]]
  for = "/*"
  [headers.values]
    Access-Control-Allow-Origin = "*"
