[build]
  publish = "."
  functions = "netlify/functions"

[functions]
  node_bundler = "esbuild"

[functions."claude"]
  timeout = 90

[[headers]]
  for = "/*"
  [headers.values]
    Access-Control-Allow-Origin = "*"
